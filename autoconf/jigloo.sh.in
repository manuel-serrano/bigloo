#!/bin/sh

path=`realpath $0`
dir=`dirname $path`

classpath=$dir:$PWD:@ZIP_DIRECTORY@

if [ " $1" = " -cp" ]; then
  classpath="$classpath:$2"
  shift;
  shift
elif [ " $1" = " -classpath" ]; then
  classpath="$classpath:$2"
  shift;
  shift
fi

@BGL_JAVA@ -cp $classpath jigloo $@
