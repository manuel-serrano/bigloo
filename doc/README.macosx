Bigloo MacOS X support is complete. That is, all Bigloo features are
supported on this platform.

In order to build a MacOS dmg file, proceed as follows:

  % tar xvfz bigloo<VERSION>.tar.gz
  % cd bigloo<VERSION>
  % make -f arch/macosx/Makefile.macosx all


