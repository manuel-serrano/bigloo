diff -Naur gc-7.2alpha4.orig/Makefile.in gc-7.2alpha4/Makefile.in
--- gc-7.2alpha4.orig/Makefile.in	2009-10-19 12:56:20.000000000 +0200
+++ gc-7.2alpha4/Makefile.in	2010-10-02 05:56:34.000000000 +0200
@@ -156,7 +156,7 @@
 	$(LIBTOOLFLAGS) --mode=link $(CCLD) $(AM_CFLAGS) $(CFLAGS) \
 	$(libcord_la_LDFLAGS) $(LDFLAGS) -o $@
 am__DEPENDENCIES_1 =
-am__libgc_la_SOURCES_DIST = allchblk.c alloc.c blacklst.c checksums.c \
+am__libgc_la_SOURCES_DIST = allchblk.c alloc.c inline_alloc.c blacklst.c checksums.c \
 	dbg_mlc.c dyn_load.c finalize.c gc_dlopen.c gcj_mlc.c \
 	headers.c malloc.c mallocx.c mark.c mark_rts.c misc.c \
 	new_hblk.c obj_map.c os_dep.c pcr_interface.c ptr_chck.c \
@@ -167,7 +167,7 @@
 @PTHREADS_TRUE@	pthread_stop_world.lo
 @DARWIN_THREADS_TRUE@am__objects_2 = darwin_stop_world.lo
 @WIN32_THREADS_TRUE@am__objects_3 = win32_threads.lo
-am_libgc_la_OBJECTS = allchblk.lo alloc.lo blacklst.lo checksums.lo \
+am_libgc_la_OBJECTS = allchblk.lo alloc.lo inline_alloc.lo blacklst.lo checksums.lo \
 	dbg_mlc.lo dyn_load.lo finalize.lo gc_dlopen.lo gcj_mlc.lo \
 	headers.lo malloc.lo mallocx.lo mark.lo mark_rts.lo misc.lo \
 	new_hblk.lo obj_map.lo os_dep.lo pcr_interface.lo ptr_chck.lo \
@@ -524,7 +524,7 @@
 	$(am__append_9) $(am__append_11)
 pkgconfigdir = $(libdir)/pkgconfig
 pkgconfig_DATA = bdw-gc.pc
-libgc_la_SOURCES = allchblk.c alloc.c blacklst.c checksums.c dbg_mlc.c \
+libgc_la_SOURCES = allchblk.c alloc.c inline_alloc.c blacklst.c checksums.c dbg_mlc.c \
 	dyn_load.c finalize.c gc_dlopen.c gcj_mlc.c headers.c malloc.c \
 	mallocx.c mark.c mark_rts.c misc.c new_hblk.c obj_map.c \
 	os_dep.c pcr_interface.c ptr_chck.c real_malloc.c reclaim.c \
@@ -765,6 +765,7 @@
 
 @AMDEP_TRUE@@am__include@ @am__quote@./$(DEPDIR)/allchblk.Plo@am__quote@
 @AMDEP_TRUE@@am__include@ @am__quote@./$(DEPDIR)/alloc.Plo@am__quote@
+@AMDEP_TRUE@@am__include@ @am__quote@./$(DEPDIR)/inline_alloc.Plo@am__quote@
 @AMDEP_TRUE@@am__include@ @am__quote@./$(DEPDIR)/alpha_mach_dep.Plo@am__quote@
 @AMDEP_TRUE@@am__include@ @am__quote@./$(DEPDIR)/atomic_ops.Plo@am__quote@
 @AMDEP_TRUE@@am__include@ @am__quote@./$(DEPDIR)/atomic_ops_sysdeps.Plo@am__quote@
diff -Naur gc-7.2alpha4.orig/alloc.c gc-7.2alpha4/alloc.c
--- gc-7.2alpha4.orig/alloc.c	2009-11-16 20:56:54.000000000 +0100
+++ gc-7.2alpha4/alloc.c	2010-10-02 05:56:34.000000000 +0200
@@ -537,6 +537,40 @@
   GC_INNER void GC_add_current_malloc_heap(void);
 #endif
 
+/*---------------------------------------------------------------------*/
+/*    Bigloo profiling 13Apr2003: start                                */
+/*---------------------------------------------------------------------*/
+static void (*GC_gcollect_hook)() = 0;
+GC_add_gc_hook( void (*f)() ) {
+   GC_gcollect_hook = f;
+}
+/*---------------------------------------------------------------------*/
+/*    Bigloo profiling 13Apr2003: stop                                 */
+/*---------------------------------------------------------------------*/
+
+/*---------------------------------------------------------------------*/
+/*    Bigloo start                                                     */
+/*---------------------------------------------------------------------*/
+static long allocated_bytes = 0;
+
+GC_get_allocated_bytes() {
+   return allocated_bytes;
+}
+
+void GC_reset_allocated_bytes() {
+   GC_gcollect();
+   allocated_bytes = 0;
+}
+
+#ifdef KEEP_BACK_PTRS
+int GC_heap_info_available = 1;
+#else
+int GC_heap_info_available = 0;
+#endif
+/*---------------------------------------------------------------------*/
+/*    Bigloo stop                                                      */
+/*---------------------------------------------------------------------*/
+
 #ifdef MAKE_BACK_GRAPH
   GC_INNER void GC_build_back_graph(void);
 #endif
@@ -576,6 +610,16 @@
         GC_cond_register_dynamic_libraries();
 #   endif
 
+/*---------------------------------------------------------------------*/
+/*    Bigloo start                                                     */
+/*---------------------------------------------------------------------*/
+#   ifdef KEEP_BACK_PTRS
+    allocated_bytes += GC_get_bytes_since_gc();
+#   endif
+/*---------------------------------------------------------------------*/
+/*    Bigloo stop                                                      */
+/*---------------------------------------------------------------------*/
+    
 #   ifndef SMALL_CONFIG
       if (GC_print_stats)
         GET_TIME(start_time);
@@ -891,6 +935,17 @@
 #       endif
     }
 
+/*---------------------------------------------------------------------*/
+/*    Bigloo profiling 13Apr2003: start                                */
+/*---------------------------------------------------------------------*/
+    if( GC_gcollect_hook )
+       GC_gcollect_hook( GC_heapsize,
+			 (unsigned long)(GC_atomic_in_use)+
+			 (unsigned long)(GC_composite_in_use) );
+/*---------------------------------------------------------------------*/
+/*    Bigloo profiling 13Apr2003: start                                */
+/*---------------------------------------------------------------------*/
+    
     /* Reset or increment counters for next cycle */
       GC_n_attempts = 0;
       GC_is_full_gc = FALSE;
diff -Naur gc-7.2alpha4.orig/dyn_load.c gc-7.2alpha4/dyn_load.c
--- gc-7.2alpha4.orig/dyn_load.c	2009-11-06 10:17:00.000000000 +0100
+++ gc-7.2alpha4/dyn_load.c	2010-10-02 05:56:34.000000000 +0200
@@ -85,11 +85,16 @@
     || (defined(__ELF__) && (defined(LINUX) || defined(FREEBSD) \
                              || defined(NETBSD) || defined(OPENBSD)))
 # include <stddef.h>
-# if !defined(OPENBSD)
+# if !defined(OPENBSD) && !defined(BGL_ANDROID)
     /* FIXME: Why we exclude it for OpenBSD? */
+    /* we exclude ANDROID because linker.h below includes its own version */
 #   include <elf.h>
 # endif
-# include <link.h>
+# if defined(BGL_ANDROID)
+#   include <linker.h>
+# else
+#   include <link.h>
+# endif
 #endif
 
 /* Newer versions of GNU/Linux define this macro.  We
@@ -572,9 +577,15 @@
 #  define PF_W         2
 #  endif
 #else
+# if !defined(BGL_ANDROID)
 #  include <elf.h>
+# endif
 #endif
-#include <link.h>
+# if defined(BGL_ANDROID)
+#   include <linker.h>
+# else
+#   include <link.h>
+# endif
 
 # endif
 
diff -Naur gc-7.2alpha4.orig/include/private/gcconfig.h gc-7.2alpha4/include/private/gcconfig.h
--- gc-7.2alpha4.orig/include/private/gcconfig.h	2009-11-06 12:12:00.000000000 +0100
+++ gc-7.2alpha4/include/private/gcconfig.h	2010-10-02 05:56:34.000000000 +0200
@@ -1845,7 +1845,8 @@
 #            define DYNAMIC_LOADING
 #            include <features.h>
 #            if defined(__GLIBC__) && __GLIBC__ >= 2 \
-                || defined(PLATFORM_ANDROID)
+                || defined(PLATFORM_ANDROID) \
+                || defined(BGL_ANDROID)
 #                define SEARCH_FOR_DATA_START
 #            else
                  extern char **__environ;
diff -Naur gc-7.2alpha4.orig/misc.c gc-7.2alpha4/misc.c
--- gc-7.2alpha4.orig/misc.c	2009-11-18 10:39:08.000000000 +0100
+++ gc-7.2alpha4/misc.c	2010-10-02 06:12:25.000000000 +0200
@@ -532,6 +532,10 @@
         word dummy;
 #   endif
 
+#ifdef BGL_ANDROID
+	void *__stack_bottom;
+#endif
+	   
 #   ifdef GC_INITIAL_HEAP_SIZE
         word initial_heap_sz = divHBLKSZ(GC_INITIAL_HEAP_SIZE);
 #   else
@@ -541,6 +545,10 @@
 
     if (GC_is_initialized) return;
 
+#ifdef BGL_ANDROID
+    __stack_base__ = GC_stackbottom ? GC_stackbottom : &__stack_bottom;
+#endif
+    
     DISABLE_CANCEL(cancel_state);
     /* Note that although we are nominally called with the */
     /* allocation lock held, the allocation lock is now    */
@@ -1057,7 +1065,8 @@
 #endif
 
 #if !defined(MSWIN32) && !defined(MSWINCE) && !defined(OS2) \
-    && !defined(MACOS)  && !defined(ECOS) && !defined(NOSYS)
+   && !defined(MACOS)  && !defined(ECOS) \
+   && (!defined(NOSYS) || defined(BGL_ANDROID))
 STATIC int GC_write(int fd, const char *buf, size_t len)
 {
      int bytes_written = 0;
@@ -1092,7 +1101,7 @@
   }
 #endif
 
-#ifdef NOSYS
+#if defined(NOSYS) && !defined(BGL_ANDROID)
   STATIC int GC_write(int fd, const char *buf, size_t len)
   {
     /* No writing.  */
diff -Naur gc-7.2alpha4.orig/os_dep.c gc-7.2alpha4/os_dep.c
--- gc-7.2alpha4.orig/os_dep.c	2009-11-09 13:27:34.000000000 +0100
+++ gc-7.2alpha4/os_dep.c	2010-10-02 06:04:36.000000000 +0200
@@ -16,6 +16,10 @@
 
 #include "private/gc_priv.h"
 
+#ifdef BGL_ANDROID
+void *__stack_base__ = 1; /* initialize by GC_init */
+#endif
+
 #if defined(LINUX) && !defined(POWERPC)
 # include <linux/version.h>
 # if (LINUX_VERSION_CODE <= 0x10400)
