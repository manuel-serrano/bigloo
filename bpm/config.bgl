;*=====================================================================*/
;*    serrano/prgm/project/bigloo/5.0.x/bpm/config.bgl                 */
;*    -------------------------------------------------------------    */
;*    Author      :  Manuel Serrano                                    */
;*    Creation    :  Wed Jul  8 11:38:54 2026                          */
;*    Last change :  Wed Jul  8 15:00:28 2026 (serrano)                */
;*    Copyright   :  2026 Manuel Serrano                               */
;*    -------------------------------------------------------------    */
;*    bpm configuration                                                */
;*=====================================================================*/

;*---------------------------------------------------------------------*/
;*    config                                                           */
;*---------------------------------------------------------------------*/
(module config
   (export verbose? verbose-level verbose-level-set!
      show repository rc-directory tmp-directory))

;*---------------------------------------------------------------------*/
;*    verbose ...                                                      */
;*---------------------------------------------------------------------*/
(define verbose 0)

;*---------------------------------------------------------------------*/
;*    verbose?                                                         */
;*---------------------------------------------------------------------*/
(define (verbose? l::long)
   (>=fx verbose l))

;*---------------------------------------------------------------------*/
;*    verbose-level ...                                                */
;*---------------------------------------------------------------------*/
(define (verbose-level::long)
   verbose)

;*---------------------------------------------------------------------*/
;*    verbose-level-set! ...                                           */
;*---------------------------------------------------------------------*/
(define (verbose-level-set! l::long)
   (set! verbose l))

;*---------------------------------------------------------------------*/
;*    show ...                                                         */
;*---------------------------------------------------------------------*/
(define (show)
   (print "repository: " (trace-color 30 repository))
   (print "rc-directory: " (trace-color 30 rc-directory))
   (print "tmp-directory: " (trace-color 30 tmp-directory)))

;*---------------------------------------------------------------------*/
;*    repository ...                                                   */
;*---------------------------------------------------------------------*/
(define repository
   (or (getenv "BPM_REPOSITORY")
       "https://github.com/manuel-serrano/bpm/blob/master/packages.sexp"))

;*---------------------------------------------------------------------*/
;*    rc-directory                                                     */
;*---------------------------------------------------------------------*/
(define rc-directory
   (or (getenv "BPM_RC_DIRECTORY")
       (make-file-path (getenv "HOME") ".config" "bpm")))

;*---------------------------------------------------------------------*/
;*    tmp-directory ...                                                */
;*---------------------------------------------------------------------*/
(define tmp-directory
   (or (getenv "BPM_TMP_DIRECTORY")
       (make-file-path (os-tmp) "bpm")))
